import React from 'react';
import BackCard from '../../../Components/UI/Flipcard/BackCard/BackCard';

const Back = (props) => {
    return (
        <BackCard>
            <p>This is the back of our card</p>
        </BackCard>
    );
};

export default Back;